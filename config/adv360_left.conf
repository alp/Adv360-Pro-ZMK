# Demote left half from central to peripheral.
# The dongle is now the central device.
CONFIG_ZMK_SPLIT_ROLE_CENTRAL=n

# TEMPORARY: clear all bond/settings data on first boot, then remove this line
CONFIG_ZMK_SETTINGS_RESET_ON_START=y
